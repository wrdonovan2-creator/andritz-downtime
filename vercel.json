{
  "buildCommand": "npm run build:frontend && npm run build:api",
  "outputDirectory": "dist/public",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" }
  ]
}
